{
  "//": "Standalone typecheck/test config for @bicharts/chart-host. The Power BI visual does NOT use this file — it compiles the package sources in-place via the root tsconfig (reached through the ../chart-host/src relative imports, exactly like ../shape-core/src). This config exists so the package can be typechecked/tested independently and consumed by the MCP client. moduleResolution 'bundler' allows the extensionless relative imports the sources use.",
  "compilerOptions": {
    "target": "es2022",
    "module": "esnext",
    "moduleResolution": "bundler",
    "lib": ["es2022", "dom"],
    "strict": false,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,
    "declaration": true,
    "noEmit": true
  },
  "include": ["src/**/*.ts"]
}
